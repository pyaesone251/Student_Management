<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_school_teacher_form" model="ir.ui.view">
        <field name="name">school.teacher.form</field>
        <field name="model">school.teacher</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Eg.Daw Mya"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id"/>
                            <field name="phone" />
                            <field name="email" />
                        </group>
                        <group>
                            <field name="date_joined"/>
                            <field name="subject_id" widget="many2many_tags" />
                            <field name="active" invisible="True" />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_school_teacher_list" model="ir.ui.view">
        <field name="name">school.teacher.list</field>
        <field name="model">school.teacher</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" />
                <field name="employee_id"/>
                <field name="phone" />
                <field name="email" />
                <field name="date_joined"/>
                <field name="subject_id" widget="many2many_tags" />
                <field name="active" invisible="True" />
            </list>
        </field>
    </record>

    <record id="action_school_teacher_view" model="ir.actions.act_window">
        <field name="name">School Teacher</field>
        <field name="res_model">school.teacher</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Teacher
            </p>
        </field>
    </record>
</odoo>